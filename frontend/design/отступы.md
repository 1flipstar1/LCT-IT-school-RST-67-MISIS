# Отступы

Правила отступов интерфейса соответствуют гайду Дизайн-системы Ростелекома 2-го поколения (Атомаро): https://design.rt.ru/gen2/basics/typography (раздел «Отступы»).

## Принцип

- Токены отступов **spacing** вычисляются из базового значения и образуют фиксированную шкалу.
- Токен привязан к модулю **4px** при `spacing.base = 16px`:
  - `spacing.0,5x` — половина модуля = 2px;
  - `spacing.2x` — два модуля = 8px;
  - и т.д.
- Использование случайных величин запрещено: все padding/margin задаются только токенами шкалы.

## Шкала отступов

| Дизайн-токен | Значение | rem (base 16px) | Pixels |
|---|---|---|---|
| spacing.px | 1 | — | 1px |
| spacing.0,5x | base × 0.125 | 0.125rem | 2px |
| spacing.1x | base × 0.25 | 0.25rem | 4px |
| spacing.1,5x | base × 0.375 | 0.375rem | 6px |
| spacing.2x | base × 0.5 | 0.5rem | 8px |
| spacing.3x | base × 0.75 | 0.75rem | 12px |
| spacing.4x | base | 1rem | 16px |
| spacing.5x | base × 1.25 | 1.25rem | 20px |
| spacing.6x | base × 1.5 | 1.5rem | 24px |
| spacing.8x | base × 2 | 2rem | 32px |
| spacing.10x | base × 2.5 | 2.5rem | 40px |
| spacing.12x | base × 3 | 3rem | 48px |
| spacing.16x | base × 4 | 4rem | 64px |
| spacing.20x | base × 5 | 5rem | 80px |
| spacing.24x | base × 6 | 6rem | 96px |
| spacing.32x | base × 8 | 8rem | 128px |
| spacing.48x | base × 12 | 12rem | 192px |

## CSS-токены

Определены в `:root` в `src/styles.css`:

```css
:root{
  --space-px:1px;
  --space-0_5x:2px;   /* spacing.0,5x */
  --space-1x:4px;     /* spacing.1x  */
  --space-1_5x:6px;   /* spacing.1,5x */
  --space-2x:8px;     /* spacing.2x  */
  --space-3x:12px;    /* spacing.3x  */
  --space-4x:16px;    /* spacing.4x  = base */
  --space-5x:20px;    /* spacing.5x  */
  --space-6x:24px;    /* spacing.6x  */
  --space-8x:32px;    /* spacing.8x  */
  --space-10x:40px;   /* spacing.10x */
  --space-12x:48px;   /* spacing.12x */
  --space-16x:64px;   /* spacing.16x */
  --space-20x:80px;   /* spacing.20x */
  --space-24x:96px;   /* spacing.24x */
  --space-32x:128px;  /* spacing.32x */
  --space-48x:192px;  /* spacing.48x */
}
```

## Маппинг компонентов на токены

### Каркас

| Элемент | Свойство | Токен |
|---|---|---|
| `.content` — контентная область | padding | 24px 24px 32px (`6x 6x 8x`) |
| `.sidebar` | padding | 24px 16px (`6x 4x`) |
| `.page-header` | margin-bottom | 24px (`6x`) |
| `.page-header h1` | margin-bottom | 8px (`2x`) |
| `.page-actions` | gap | 12px (`3x`) |
| `.filter-strip` | gap / margin-bottom | 24px (`6x`) |
| `.kpi-grid` | gap | 16px (`4x`) |
| `.dashboard-grid`, `.report-cards`, `.catalog-grid` | gap | 16px (`4x`) |
| `.donut-wrap` | gap | 64px (`16x`) |

### Карточки и панели

| Элемент | Свойство | Токен |
|---|---|---|
| `.kpi` | gap / padding | 12px (`3x`) / 20px (`5x`) |
| `.attention-panel`, `.big-chart`, `.ranking` | padding | 24px (`6x`) |
| `.legend div` | padding | 8px 0 (`2x`) |
| `.activity` | gap | 12px (`3x`) |
| `.attention-row` | gap / padding | 16px (`4x`) / 12px 0 (`3x`) |
| `.rank` | gap / margin | 8px (`2x`) / 24px 0 (`6x`) |
| `.line-chart` | padding | 24px 0 0 40px (`6x … 10x`) |

### Таблицы и тулбары

| Элемент | Свойство | Токен |
|---|---|---|
| `.toolbar`, `.workflow-toolbar` | gap / margin-bottom | 8px (`2x`) / 20px (`5x`) |
| `.table-meta`, `.pagination` | padding | 0 24px (`6x`) |
| `th` | padding | 12px 24px (`3x 6x`) |
| `td` | padding | 16px 24px (`4x 6x`) |
| `.status-tag` | padding | 4px 8px (`1x 2x`) |
| `.count-badge` | padding | 4px 6px (`1x 1,5x`) |
| `.col-count` | padding | 2px 4px (`0,5x 1x`) |
| `.uni-cell`, `.person`, `.text-button` | gap | 12px / 8px / 8px |

### Workflow-доска

| Элемент | Свойство | Токен |
|---|---|---|
| `.board-col` | padding | 12px (`3x`) |
| `.card-title` | margin | 8px 0 (`2x`) |
| `.card-foot` | margin-top / padding-top | 12px (`3x`) / 8px (`2x`) |
| `.add-card` | margin | 12px 2px (`3x 0,5x`) |

### Импорт данных

| Элемент | Свойство | Токен |
|---|---|---|
| `.import-steps` | margin | 24px auto 32px (`6x … 8x`) |
| `.import-steps div` | gap | 8px (`2x`) |
| `.import-panel` | padding | 40px (`10x`) |
| `.upload-zone` | padding | 48px 20px (`12x 5x`) |
| `.upload-zone h2` | margin | 16px 0 4px (`4x … 1x`) |
| `.import-note` | gap / padding / margin-top | 8px (`2x`) / 12px (`3x`) / 16px (`4x`) |
| `.mapping>div` | gap / padding | 12px (`3x`) / 12px 0 (`3x`) |
| `.validation` | gap / margin | 48px (`12x`) / 32px 0 (`8x`) |
| `.result-numbers` | gap / margin | 64px (`16x`) / 32px 0 (`8x`) |

### Модальные окна и меню

| Элемент | Свойство | Токен |
|---|---|---|
| `.modal` | padding | 24px (`6x`) |
| `.modal-heading` | gap | 12px (`3x`) |
| `.modal-close` | right / top | 20px (`5x`) |
| `.modal-stats` | gap / margin / padding | 16px (`4x`) / 24px 0 (`6x`) / 16px 0 (`4x`) |
| `.modal-footer` | gap / margin-top | 8px (`2x`) / 24px (`6x`) |
| `.menu-popup` | padding | 4px (`1x`) |
| `.menu-item` | padding | 8px 12px (`2x 3x`) |
| `.toast` | bottom / right / padding | 24px (`6x`) / 32px (`8x`) / 12px 16px (`3x 4x`) |

### Сайдбар

| Элемент | Свойство | Токен |
|---|---|---|
| `.workspace` | padding | 16px 12px (`4x 3x`) |
| `.brand` | padding | 0 12px 24px (`3x … 6x`) |
| `nav` | padding-top | 24px (`6x`) |
| `.nav-label` | padding / margin-top | 0 12px 8px (`3x 2x`) / 16px (`4x`) |
| `.nav-item` | padding | 8px 12px (`2x 3x`) |
| `.help-card` | gap / padding / margin | 8px (`2x`) / 12px (`3x`) / 16px 4px 20px (`4x 1x 5x`) |
| `.user-mini` | padding | 16px 8px 0 (`4x 2x`) |

## Правила использования

1. Отступы задаются только токенами шкалы `--space-*` — никаких произвольных значений.
2. `spacing.4x` (16px) — базовый модуль; для плотных интерфейсов допустимо уменьшение до `2x` (8px), для просторных — увеличение до `6x`–`8x`.
3. Внутренние отступы компонентов описываются компонентными токенами, которые ссылаются на токены spacing (в проекте — напрямую `var(--space-*)`).
4. Не использовать значения вне шкалы (например 10px, 13px, 15px, 17px, 22px, 25px, 30px, 36px, 50px, 55px, 60px, 66px…).
5. Для размеров элементов, не являющихся отступами (высоты кнопок, ширины колонок, радиусы), шкала spacing не применяется.