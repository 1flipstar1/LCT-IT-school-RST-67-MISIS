import React from 'react';
import { IIconProps } from '../../types';
declare const FastForward30s: {
    (props: IIconProps): React.JSX.Element;
    displayName: string;
};
export default FastForward30s;
