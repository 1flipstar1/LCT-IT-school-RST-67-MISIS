import React from 'react';
import { IIconProps } from '../../types';
declare const FaceOpenMouth: {
    (props: IIconProps): React.JSX.Element;
    displayName: string;
};
export default FaceOpenMouth;
