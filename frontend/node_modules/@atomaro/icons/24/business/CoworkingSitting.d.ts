import React from 'react';
import { IIconProps } from '../../types';
declare const CoworkingSitting: {
    (props: IIconProps): React.JSX.Element;
    displayName: string;
};
export default CoworkingSitting;
