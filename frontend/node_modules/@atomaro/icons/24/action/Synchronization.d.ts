import React from 'react';
import { IIconProps } from '../../types';
declare const Synchronization: {
    (props: IIconProps): React.JSX.Element;
    displayName: string;
};
export default Synchronization;
