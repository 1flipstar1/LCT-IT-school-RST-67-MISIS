import React from 'react';
import { IIconProps } from '../../types';
declare const SecurityCheck: {
    (props: IIconProps): React.JSX.Element;
    displayName: string;
};
export default SecurityCheck;
