import React from 'react';
import { IIconProps } from '../../types';
declare const GalleryCards: {
    (props: IIconProps): React.JSX.Element;
    displayName: string;
};
export default GalleryCards;
