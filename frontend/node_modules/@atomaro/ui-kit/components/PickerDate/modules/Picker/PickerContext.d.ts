import { ReactNode, CSSProperties } from 'react';
import { CALENDAR_VIEW, CALENDAR_MODE, PickerDateVariants, PickerDateSizes } from '../../constants';
type PickerContextType = {
    variant: PickerDateVariants | string;
    size: PickerDateSizes | string;
    view: CALENDAR_VIEW;
    date: Date;
    activeDate?: Date;
    secondDate?: Date;
    today: Date;
    isRange: boolean;
    setDate: Function;
    nameOfMonths: string[];
    daysOfWeek: string[];
    className?: string;
    style?: CSSProperties;
    dayClassName?: string;
    dayStyle?: CSSProperties;
    dateClickHandler: (date: Date) => void;
    changeView: (view: CALENDAR_VIEW) => void;
    renderDate?: (date: Date, view?: CALENDAR_VIEW) => ReactNode;
    calendarMode: CALENDAR_MODE;
};
export declare const PickerContext: import("react").Context<PickerContextType>;
export {};
